# Shared defaults can live in the repo root `.env`.
# Values in `proposal/.env` override the repo root for proposal-only jobs.

EXA_API_KEY=

# Choose one persistence mode.
DATABASE_URL=
POSTGRES_URL=

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
SUPABASE_KEY=

