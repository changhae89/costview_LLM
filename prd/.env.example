# Shared defaults can live in the repo root `.env`.
# Values in `prd/.env` override the repo root for PRD-only jobs.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Choose one persistence mode.
DATABASE_URL=
POSTGRES_URL=

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
SUPABASE_KEY=
